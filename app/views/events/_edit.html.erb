  <% remote_form_for event, :with => %Q{btn=$(#) } do |f| -%>
    <%= f.text_field :artist -%>
    <%= f.text_field :city -%>
    <%= f.text_field :date -%>
    <%= f.text_field :club -%>
    <%= f.text_field :price -%>
    <%= f.text_field :id %>
    <%= hidden_field_tag 'next_action', '' %>
    <%= f.submit t("actions.new"),    { :onclick => %Q{ $('#next_action').val('new')    }} %>
    <%= f.submit t("actions.update"), { :onclick => %Q{ $('#next_action').val('update') }} %>
    <%= f.submit t("actions.prev"),   { :onclick => %Q{ $('#next_action').val('prev')   }} %>
    <%= f.submit t("actions.next"),   { :onclick => %Q{ $('#next_action').val('next')   }} %>
  <% end %>
