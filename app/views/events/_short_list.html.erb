  <div class="top-line"></div>
  <% events.each do |event| -%>
    <div id="event_<%= event.id -%>">
      <%= link_to render( :partial => '/events/short_list_element', :locals => { :event => event } ), event.link || events_url %>
    </div>
  <% end -%>
